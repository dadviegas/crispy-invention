{
  "name": "site",
  "description": "Site app description",
  "version": "1.0.0",
  "main": "index.js",
  "author": "dadviegas",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "webpack",
    "start": "webpack-dev-server --open --hot --inline",
    "build:dev": "yarn clean & webpack",
    "build:production": "yarn clean & webpack --mode=production --env.country=pt",
    "build:all": "yarn clean & webpack --config=webpack.config.all.babel.js --profile"
  },
  "fependencies": {
    "react-bundle": "0.0.1"
  },
  "devDependencies": {
    "webpack-conf": "0.0.2"
  }
}
